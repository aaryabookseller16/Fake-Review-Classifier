# Pinned to the versions these results were produced with.
# Reproduce the benchmark table in the README with:
#   PYTHONPATH=src python -m fakereview.train

# Core numerics / data
numpy==2.5.2
pandas==3.0.5
scipy==1.18.1

# Modelling + persistence
scikit-learn==1.9.0
joblib==1.6.0

# Web app
streamlit==1.62.0
altair==6.2.2
